#pragma once

class Vec3
{
  public:
  // constructors
  Vec3(double x, double y, double z) : x(x), y(y), z(z) {}

  // attributes
  double x, y, z;
};

class Vec2
{
  public:
  // constructors
  Vec2(double x, double y) : x(x), y(y) {}

  // attributes
  double x, y;
};